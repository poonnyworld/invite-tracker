# 🔧 แก้ไขปัญหา Google Sheets ไม่แสดงข้อมูล

## ⚠️ ปัญหาที่พบ

- Discord Dashboard แสดงข้อมูลถูกต้อง (เช่น "Timmy • 1 invites")
- แต่ Google Sheets แสดง "No data available" หลังจาก Update All Sheets

## 🔍 สาเหตุที่เป็นไปได้

### 1. Configuration ไม่ถูกต้อง

Google Apps Script ใช้ placeholder ที่ต้องแก้ไข:

```javascript
const API_URL = 'http://YOUR_SERVER_IP:3001'; // ต้องแก้ไข!
const GUILD_ID = 'YOUR_DISCORD_GUILD_ID'; // ต้องแก้ไข!
```

**วิธีแก้ไข:**
1. เปิด Google Sheet → Extensions → Apps Script
2. แก้ไข `API_URL` และ `GUILD_ID` ให้ตรงกับของคุณ:
   ```javascript
   const API_URL = 'http://143.14.200.26:3001'; // IP address ของคุณ
   const GUILD_ID = '1098556040439660616'; // Guild ID ของคุณ
   ```

### 2. CORS Error

Google Apps Script ไม่สามารถเรียก API ได้เพราะ CORS

**วิธีแก้ไข:**
- ตรวจสอบว่า `ALLOWED_ORIGINS` ใน `api/.env` มี `https://script.google.com`
- Restart API server: `docker-compose restart invite-tracker-api`

### 3. API ไม่สามารถเข้าถึงได้

Google Apps Script ไม่สามารถเข้าถึง API server จากภายนอก

**วิธีแก้ไข:**
- ตรวจสอบว่า API server ทำงานอยู่: `curl http://143.14.200.26:3001/api/health`
- ตรวจสอบว่า firewall เปิดพอร์ต 3001
- ตรวจสอบว่า API_URL ถูกต้อง

### 4. Guild ID ไม่ถูกต้อง

Guild ID ที่ใช้ใน Google Apps Script ไม่ตรงกับ Discord Server

**วิธีแก้ไข:**
- ตรวจสอบ Guild ID จาก Discord (Developer Mode → Right click server → Copy Server ID)
- อัพเดต `GUILD_ID` ใน Google Apps Script

## 🧪 ทดสอบการเชื่อมต่อ

ใน Google Apps Script Editor:

1. เลือก function `testConnection`
2. คลิก Run
3. ดู Logs (View → Logs) เพื่อตรวจสอบว่าเชื่อมต่อ API ได้หรือไม่

## 📋 Checklist

- [ ] แก้ไข `API_URL` ใน Google Apps Script ให้เป็น IP address จริง
- [ ] แก้ไข `GUILD_ID` ใน Google Apps Script ให้เป็น Guild ID จริง
- [ ] ตรวจสอบว่า `ALLOWED_ORIGINS` มี `https://script.google.com`
- [ ] Restart API server
- [ ] ทดสอบการเชื่อมต่อด้วย `testConnection()`
- [ ] ตรวจสอบ Logs ใน Google Apps Script

## 🔄 หลังจากแก้ไข

1. รัน `updateAllSheets()` อีกครั้ง
2. ตรวจสอบ Logs เพื่อดู error messages
3. ถ้ายังมีปัญหา ให้ดู Execution logs (View → Executions)

## 💡 Tips

- ใช้ `updateSummarySheet()` เพื่อทดสอบก่อน (เร็วกว่า)
- ตรวจสอบ Logs ทุกครั้งที่รัน script
- ถ้า API ไม่สามารถเข้าถึงได้ ให้ตรวจสอบ firewall และ network configuration
