# Discord Bot Configuration
# Get these from https://discord.com/developers/applications

# Bot Token (Required)
# Create a bot at Discord Developer Portal and copy the token
DISCORD_TOKEN=your_bot_token_here

# Client ID (Required for deploying slash commands)
# Found in the "General Information" section of your Discord application
CLIENT_ID=your_client_id_here

# Guild ID (Optional - for faster command deployment during development)
# Right-click your Discord server > Copy Server ID
# Leave empty to deploy commands globally (takes up to 1 hour)
GUILD_ID=your_guild_id_here

# Dashboard Channel ID (Required)
# Right-click the channel where you want the dashboard to appear > Copy Channel ID
INVITE_DASHBOARD_CHANNEL_ID=your_dashboard_channel_id_here

# MongoDB Configuration
# For local MongoDB: mongodb://localhost:27017/honorbot
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/honorbot
# For Docker: mongodb://mongodb:27017/honorbot (automatically set by docker-compose.yml)
MONGO_URI=mongodb://localhost:27017/honorbot

# API Configuration (Optional)
# URL of the API server (if running separately)
# Leave empty if not using external API
API_URL=http://localhost:3001

# API Secret Key (Optional)
# Used for authenticating API requests from the bot
# Should match API_SECRET_KEY in api/.env if using external API
API_SECRET_KEY=your_api_secret_key_here

# Environment
# Set to "production" for production deployment
NODE_ENV=development
